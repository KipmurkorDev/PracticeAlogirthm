def spiral(sides):
    for n in range(sides):
        t.forward(n)
        t.right(30)

spiral(50)
// equivalent code in JavaScript
var count = 1; 
var name = "Julia";
var num = 1.2932;
var price = 2.99;